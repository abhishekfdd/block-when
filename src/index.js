/**
 * Block When — editor entry point.
 *
 * Registers the higher-order component on `editor.BlockEdit`, the
 * `editor.BlockListBlock` indicator filter, and the built-in JS-side
 * conditions used for live preview in the editor.
 *
 * @package Block_When
 */
