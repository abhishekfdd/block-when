/**
 * Block When — date-range condition (editor).
 *
 * Editor-side counterpart of `Date_Range_Condition`. Provides the
 * datetime pickers and a live-preview evaluator.
 *
 * @package Block_When
 */
