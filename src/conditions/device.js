/**
 * Block When — device condition (editor).
 *
 * Editor-side counterpart of `Device_Condition`. Provides the device-type
 * checkbox UI and a live-preview evaluator (driven by the editor's
 * "preview as" toggle).
 *
 * @package Block_When
 */
