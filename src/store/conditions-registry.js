/**
 * Block When — JS conditions registry.
 *
 * Mirror of the PHP `Conditions_Registry` for the editor. Stores the
 * id, label, schema, and (optional) live-preview evaluator for each
 * registered condition.
 *
 * @package Block_When
 */
