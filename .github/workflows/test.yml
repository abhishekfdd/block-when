name: Test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    # Implementation deferred — matrix should cover PHP 7.4, 8.0, 8.1, 8.2, 8.3
    # per the submission checklist in PLAN.md.
    steps:
      - run: 'true'
